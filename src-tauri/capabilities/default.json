{
  "$schema": "https://schemas.tauri.app/2/capability",
  "identifier": "default",
  "description": "Default capabilities for the launcher",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:allow-open",
    "shell:allow-execute"
  ]
}
